<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<!--mapper/user.xml实现mapper/UserMapper接口里的具体方法，所以这里的namespace是com.csranger.house.mapper.UserMapper文件，将
UserMapper接口和user.xml具体实现关联起来-->
<mapper namespace="com.csranger.house.biz.mapper.UserMapper">

    <!-- result 这里可简写为user，因为Mybatis配资文件中使用了typeAlias resultType将查询结果银蛇到user对象-->
    <!--id就是实现接口方法的方法名-->
    <select id="selectUsers" resultType="User">
        select id, name, email, phone from user
    </select>

</mapper>